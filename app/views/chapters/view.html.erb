<br>
<%= link_to("Back to " + @tut.title, "/tutorials/#{@tut.id}") %>
<h1>Tutorial view: <%= @tut.title %></h1>
<h2>Chapter view: <%= @ch.title %></h2>
<% if @ch.draft == true %>
  <p>This is in Draft Status</p>
<% else %>
<% end %>
<p>Chapter Description: <%= @ch.description %></p>

<h2>View the steps:</h2>
<ol>
  <% @steps.each do |step| %>
    <li><%= step.title %>
      <p><%= step.description %></p>
    </li>
    <br>
  <% end %>
</ol>




   <%= link_to "Delete", @chapter, method: :delete,
                                  data: { confirm: "Are you sure you want to delete this chapter?" } %>